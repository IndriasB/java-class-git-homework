public static Boolean scoresIncreasing(int[] scores) {

  boolean isIncreasing = true;
  for (int i = 0; i < 10; ++i)
  while ((increasing == true) && (i < (scores.length-1))) {
     if (scores[i] < scores[i+1])
    
       increasing = true;
     
}
}
}